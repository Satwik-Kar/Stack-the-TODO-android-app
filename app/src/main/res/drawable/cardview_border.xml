<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/primarySurface" /> <!-- Background color of the CardView -->
    <corners android:radius="20dp" /> <!-- Corner radius for rounded corners -->
    <stroke
        android:width="1dp"
        android:color="@color/primary" /> <!-- Stroke width and color -->
</shape>
